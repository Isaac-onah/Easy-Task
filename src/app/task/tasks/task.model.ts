
// Define the Task interface to represent the structure of a task object
// This interface includes properties such as id, userId, title, summary, and dueDate
export interface Task {id: string, userId: string, title: string, summary: string, dueDate: string}