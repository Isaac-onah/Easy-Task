

// Define the User interface to represent the structure of a user object
// This interface includes properties such as id, avatar, and name  
export interface User {id: string, avatar: string, name: string }